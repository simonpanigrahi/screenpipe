# System dependencies — NOT a pip requirements file.
# GStreamer and its Python bindings must be installed via apt, not pip.
# Run the commands below on Ubuntu 24.04 (GNOME / Wayland).
#
# Usage:
#   sudo apt update
#   sudo apt install -y $(grep -v '^#' requirements.txt | grep -v '^$' | tr '\n' ' ')
#
# (Each line below is an apt package name, commented for documentation.)

# ── Python + GObject / D-Bus bindings ────────────────────────────────────────
# python3
# python3-gi
# python3-pydbus
# gir1.2-gtk-3.0

# ── GStreamer core + plugins ──────────────────────────────────────────────────
# gstreamer1.0-tools
# gstreamer1.0-plugins-base
# gstreamer1.0-plugins-good
# gstreamer1.0-plugins-bad
# gstreamer1.0-plugins-ugly
# gstreamer1.0-pipewire
# gstreamer1.0-libav

# ── Hardware encoder plugins (install the one that matches your GPU) ──────────
# Intel / AMD (VA-API):
#   gstreamer1.0-vaapi
#   va-driver-all
# NVIDIA (NVENC) — needs proprietary driver; nvh264enc ships in plugins-bad
#   (no extra apt package; ensure the proprietary driver is installed first)

# ── ADB (USB tunnel) ─────────────────────────────────────────────────────────
# adb
